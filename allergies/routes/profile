var express = require('express');
var router = express.Router();

var user = 'chepa'//Load user data here

/* GET profile page. */
router.get('/', function(req, res, next) {
  res.render('profile', { title: 'Profile', user: 'Username' });
});

module.exports = router;